# Private. Never committed.
DESIGN.md
.claude/rules/private.md
.claude/settings.local.json
.env

# Workspace files. A workspace is created by /coldwriter:init, is never a git repo,
# and never lives inside this checkout; these keep an accidental one out of a commit
# while `make guard` fails on it.
/me/
/constitution.md
/queue.md
/learning-curve.svg
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Tooling
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
evals/results/
.DS_Store
